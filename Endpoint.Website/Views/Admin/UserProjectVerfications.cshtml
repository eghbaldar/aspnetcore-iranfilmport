@*
    DeVeloped: Alimohammad Eghbaldar
    www.eghbaldar.ir
    www.iranfilmport.com
*@
@using IranFilmPort.Application.Services.UserProjects.Queries.GetAllUserProjectsForAdmin
@using IranFilmPort.Common.Constants
@using IranFilmPort.Common.Helpers
@{
    Layout = "~/views/shared/_LayoutUserClientAdmin.cshtml";
}
@model ResultGetGetAllUserProjectsForAdminServiceDto
<div class="row">
    <div class="col-lg-12">
        <div class="card-box">
            <h4 class="mt-0 header-title">جدول پروژه های کاربران</h4>
            <div class="table-responsive">
                <table class="table mb-0" id="tb">
                    <thead>
                        <tr>
                            <th>نام</th>
                            <th></th>
                            <th></th>
                            <th>تاریخ درج</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Result)
                        {
                            <tr>
                                <td>
                                    <a class="kingBlueText" href="/admin/adminUsers/@item.UserId" target="_blank">@item.Fullname</a>
                                </td>
                                <td>
                                    <a href="/admin/userproject/@item.Id" target="_blank">{مشاهده پروژه}</a>
                                </td>
                                <td>
                                    <button type="button" onclick="changestatus('@item.Id','@StatusConstants.Rejected');" class="btn btn-primary btnWaiting" data-dismiss="modal">رد</button>
                                    <button type="button" onclick="changestatus('@item.Id','@StatusConstants.Accepted');" class="btn btn-primary btnWaiting" data-dismiss="modal">تائید</button>
                                    <button type="button" onclick="changestatus('@item.Id','3');" class="btn btn-warning btnWaiting" data-dismiss="modal">نماد</button>
                                </td>
                                <td>
                                    @{
                                        var shamsiDate = new PersianDateTime(Convert.ToDateTime(item.InsertDateTime));
                                        var shamsiDateString = shamsiDate.ToString("yyyy/MM/dd");
                                        var persianDateString = PersianNumberHelper.ConvertToPersian(shamsiDateString);
                                    }
                                    @persianDateString
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script src="~/admintemplate/pagecssjs/userProjectVerfications/userProjectVerfications.js"></script>
}